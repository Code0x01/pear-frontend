import React from "react";
import { Container } from "reactstrap";
import "./style.css";

const Orders = props => {
	return (
		<Container>
			<p>Orders page</p>
		</Container>
	);
};

export default Orders;